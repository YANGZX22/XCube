# Changelog

## v1.1.0 (2026-04-30)

这一版从 `1.0.13` 继续往前收了一轮主体验：新增助手模块，把多助手管理、自定义助手头像、默认模型和系统提示词这些能力集中起来；聊天、设置与模型管理等高频页面统一换成更贴近 HarmonyOS 的 HDS 材质与渐变模糊；主题配色也重新整理，新增 Notion 主题。推理控制按不同服务商能力做了更细的适配，导入导出策略同步调整为恢复后可直接使用，为后续 WebDAV 全量同步打基础。

### ✨ 新增 / 改进

- 新增助手模块，支持在首页侧边入口管理多个助手，并按助手查看会话、快速创建专属对话
- 助手模块内支持自定义助手头像，可使用文字、图片链接、QQ 头像、本地图库图片和默认模型图标，列表页、详情页和编辑页的头像展示尺寸更统一
- 助手模块支持默认模型、系统提示词、温度覆盖等信息，默认助手和自定义助手的编辑、删除、排序与会话关联更清楚
- 聊天卡片页顶部接入 `HdsNavigation` 材质导航，搜索、收藏、标题栏与顶部渐变模糊范围重新整理，滚动时顶部质感更稳定
- 聊天 Tab 收敛为单 `HdsNavigation` 自适应分栏，大屏空态、返回路径和列表 / 详情切换更稳定
- 设置、模型管理、导入导出、关于、通知等二级页统一接入 HDS 顶部模糊效果，页面切换时材质表现更一致
- 主题配色重新整理，新增 Notion 极简主题，默认暖纸、Claude、ChatGPT、Violet、Mint 等主题的消息、卡片、Markdown 和桌面小组件配色更协调
- 首页筛选交互收敛，弱化「全部 / 收藏」Tab 的打扰，搜索展开与返回后的标题栏状态更稳
- 底部 / 侧边 Tab 补齐点击即时缩放反馈，助手 Tab 的选中态图标染色与对话页保持一致
- 推理控制按模型和服务商能力细分，补齐自动、低、中、高、最高等推理级别，并兼容更多 OpenAI 兼容服务商策略
- 新增小米 MiMo 预置服务商，并补齐 MiMo thinking / reasoning 兼容判断
- 导出数据默认包含 API Key、搜索服务和 MCP 连接配置，导入后可直接使用，同时在导出页明确提示备份文件需要保存到可信位置

### 🐛 修复

- 修复助手头像编辑后列表、设置页和对话入口不及时刷新的问题
- 修复聊天卡片页搜索展开后新建对话、返回列表时顶部搜索和收藏入口丢失的问题
- 修复搜索展开动画在状态恢复后缺失的问题
- 修复聊天卡片页左上角助手头像与助手设置页头像尺寸不一致的问题
- 修复会话列表多选、筛选和收藏状态切换时部分临时状态未正确回收的问题
- 修复推理级别在部分服务商和模型组合下可用性判断不准确的问题
- 修复部分组件仍读取旧主题色导致切换主题后颜色不一致的问题
- 修复后台任务完成后通知未及时清理的问题

### 🔧 内部优化

- 新增 `AssistantService`、助手头像缓存与助手管理页，减少首页对助手数据和编辑状态的直接耦合
- 补充助手、头像、服务商、偏好设置在导入导出中的结构，为 WebDAV 全量快照同步预留统一备份基础
- 抽出 HDS 顶部模糊、点光效果和聊天顶部导航相关状态，减少导航栏、搜索栏、筛选栏之间的布局联动成本
- 重整主题色服务与全局 UI 状态，减少主题资源、页面硬编码颜色和桌面小组件之间的同步成本
- 扩展 `ProviderCompatibilityRegistry`，把不同服务商的推理请求策略集中维护，降低发送链路里的特殊判断

## v1.0.13 (2026-04-24)

这一版聚焦聊天输入和消息复制两条高频体验，同时把 MCP 工具发送流程、首页悬浮按钮和主题色透明度处理继续收紧。整体变化不大，但都落在每天会碰到的细节上。

### ✨ 新增 / 改进

- 新增「聊天输入」设置页，可选择键盘回车键行为：默认换行，或开启后直接作为发送按钮
- 助手消息新增原地文本选择模式，可通过长按消息内容或更多菜单里的「选择文本」进入
- 文本选择模式下会保留 Markdown 的基础阅读样式，并在复制时整理为更干净的纯文本
- 首页「新对话」悬浮按钮调整为更大的圆形按钮，补齐按压弹簧动画、阴影和底部占位
- 主题色透明度拼接统一改为 `withColorAlpha` 工具，输入框、消息气泡、工具中心和滚动按钮等状态色更稳定

### 🐛 修复

- 修复助手消息分段复制不完整、富文本选择状态样式不统一的问题
- 修复选择文本弹窗颜色资源和按钮布局细节，选择态气泡更清楚
- 修复 MCP 工具未完全就绪时发送被直接拦截的问题，改为后台刷新可用状态
- 修复部分主题色带透明度时在不同组件中显示不一致的问题

### 🔧 内部优化

- 新增 `SelectableMarkdownContent`，把选择态 Markdown 渲染和纯文本提取逻辑独立出来
- 聊天输入设置接入 `PreferencesService`、`SettingsManager` 和全局 UI 状态，保证设置即时生效
- `docs/appgallery` 加入忽略，避免应用市场截图产物误提交

## v1.0.12 (2026-04-13)

这一版继续围绕高频路径做收口，不是单点功能堆砌，而是把聊天详情页标题栏、多选链路、设置下钻页、图片预览、附件面板和视觉材质这些每天都会碰到的地方再打磨一轮。整体方向很明确：继续基于鸿蒙 6（API 23）把常用路径做顺、做稳、做干净。

### ✨ 新增 / 改进

- 聊天详情页标题栏完成一轮重构，HDS 风格布局、模糊范围、操作菜单、图标尺寸与滚动联动更统一
- 设置界面继续减负，下钻页隐藏不必要的 Tabs，底部占位与回退路径更清楚
- 聊天界面与消息/会话列表的多选能力进一步补齐，悬浮批量工具栏和相关交互更顺手
- 聊天页附件面板与加号切换逻辑优化，输入框回车发送能力补上
- 联网搜索能力补齐 Bing(local)、Tavily、Exa 三种执行引擎，聊天内搜索卡片会展示实际使用的搜索源
- 工具中心继续扩展，支持远端 streamable MCP Server 接入，统一承接内置工具与远端工具能力
- ChatGPT 主题与整体配色继续微调，会话列表层级和正文可读性进一步优化
- 聊天流式请求读取超时时间提升到 300 秒，长回复和慢流式场景更稳
- 增加沉浸光感材质设置项，8 种配色主题、悬浮按钮、滚动按钮、桌面卡片和整体主题视觉继续统一
- 服务商兼容层补充 OpenAI Responses transport、导入保留 transport 与排序信息，模型与服务商管理体验更完整

### 🐛 修复

- 修复聊天图片区域滑动手势被吞、图片预览状态栏切换不稳的问题
- 修复聊天界面、消息卡片和会话列表在多选模式下的一批状态错乱、布局不稳和交互别扭问题
- 修复等待 AI 回复后的 typing indicator 恢复、流式输出中消息工具栏过早出现等聊天细节问题
- 修复设置下钻页 HdsTabs 底部显示与占位异常，以及部分 overlay / blur 叠层关系不稳定的问题
- 修复冷启动时状态栏颜色与沉浸式显示偶发不一致的问题
- 修复 FAB 阴影、按钮边框激活态位移、桌面卡片深浅色跟随系统等一批视觉细节问题

### 🔧 内部优化

- 继续收缩 `ChatPage`、`Index` 和标题栏相关状态分支，降低复杂 UI 的维护成本
- Provider 导入排序、列表渲染、标题栏交互和多选逻辑补上更多回归边界
- 补充 `.gitattributes` 统一 LF 行尾，顺手清理一批没用的配置与入口

## v1.0.11 (2026-03-31)

这一版最核心的变化不是某个按钮或某个页面，而是运行门槛正式提升到了鸿蒙 6（API 23）。与此同时，首页、聊天页和服务商配置这几条高频路径也继续收紧：主界面的两栏导航更干净，遮罩和浮层不再抢戏，服务商和 MCP 都补上了更克制的自定义请求头能力。

### ✨ 新增 / 改进

- 应用运行前提调整为鸿蒙 6（API 23），`targetSdkVersion` 与 `compatibleSdkVersion` 统一升级到 `6.1.0(23)`
- 服务商级别新增自定义请求头能力，聊天发送、测试连接、获取模型、标题生成、翻译和搜索优化等链路统一生效
- MCP 工具服务的自定义请求头编辑体验同步收紧，和服务商配置保持一致
- 首页底部导航收敛为双标签布局，设置入口更集中，整体层级更清楚
- 聊天页和首页的一批遮罩、悬浮元素与覆盖层进一步压平，视觉更干净，交互也更稳
- 设置与隐私相关导航继续收口，二级页跳转更顺，回退路径更统一

### 🐛 修复

- 修复首页和聊天页多处 overlay/blur 叠层关系不稳定、偶发抢焦点和遮挡内容的问题
- 修复服务商自定义请求头开启后自动插入空白项，关闭后仍被错误计数的问题
- 修复请求头编辑区在展开后把开关位置挤偏的问题，开关位置现已固定
- 修复聊天与会话列表在高频更新场景下的一批渲染抖动、复用不稳和局部刷新噪音

### 🔧 内部优化

- 继续收缩首页、聊天页和列表项的渲染负担，减少不必要的重建与状态扩散
- 清理一批已废弃字符串资源和未使用入口，减轻资源与界面噪音
- 自定义请求头的解析、校验和敏感字段判断抽成共享工具，避免服务商与 MCP 两套逻辑继续分叉

## v1.0.10 (2026-03-20)

这一版不是修修补补，而是把工具链、列表渲染和服务商兼容层一起收了一轮：MCP 工具中心正式落地，聊天内工具调用状态更稳定，会话/消息列表的数据源与复用链路也更清晰，整体可维护性比之前高一个量级。

### ✨ 新增 / 改进

- 新增 MCP 服务接入和工具中心主流程，统一承接内置工具与远端 MCP 工具
- 聊天页补齐工具选择、审批和执行相关 UI，工具栏 pills 与工具调用卡片展示更稳定
- 推理能力配置从服务商策略中解耦，模型编辑页和聊天页按模型真实能力展示 reasoning 相关入口
- 会话列表和消息列表引入更清晰的数据源与列表项结构，长列表滚动和复用表现更稳
- 隐私政策改为应用内二级页面承载，避免跳转外部浏览器打断当前设置流
- 聊天界面补充进度圈元素，方便更直观地感知当前浏览位置
- 分享图片现在支持更长的对话内容，长对话导出不再容易被截断

### 🐛 修复

- 修复工具事件、工具状态在发送流程、落库恢复和消息 UI 之间不同步的问题
- 修复服务商兼容判断、模型获取和分享预览链路中的一批分叉问题，provider 图标和分享内容更稳定
- 修复首页 session list 更新链路里未读角标和多处冗余状态带来的刷新噪音
- 修复消息列表复用、滚动联动和部分工具消息卡片在复杂场景下的渲染异常
- 修复等待 AI 回复时 typing indicator 三点动画丢失的问题
- 修复首页搜索展开后输入框不自动聚焦、软键盘不弹出的交互回归
- 修复系统提示词、显示与外观、工具服务等设置页底部内容与 compact tabs 贴边甚至被遮挡的问题
- 修复聊天界面进度圈依赖绝对滚动偏移时在复杂列表下不够稳定的问题，改为基于消息可见位置计算

### 🔧 内部优化

- 新增 `ProviderCompatibilityRegistry`、`McpService`、列表 DataSource 等基础设施，明显减轻 `ChatPage` 和 `Index` 的状态负担
- 重构聊天工具相关 sheet 组件，减少 `ChatPage` 膨胀，降低后续继续堆特殊分支的风险
- 补充 `RefactorGuard.test.ets` 和 `ohosTest` 入口，为本轮结构调整加上基础回归边界

## v1.0.9 (2026-03-16)

这一版主要把服务商配置、聊天发送稳定性和提示反馈再收紧一轮：服务商详情页更顺手，附件重发和返回提示更可靠，MiniMax 兼容链路也补齐了。

### ✨ 新增 / 改进

- 服务商详情页的标题与地址信息对齐优化，编辑和查看状态更统一
- MiniMax 预置服务商升级到最新兼容地址，连接测试与模型获取流程更顺
- 服务商测试连接、获取模型失败时优先展示真实错误信息，排查更直接

### 🐛 修复

- 修复会话重新打开时总是回到全局当前模型的问题，改为优先恢复该会话上次使用的模型
- 修复服务商详情页标题首字显示不完整、URL 摘要未对齐的问题
- 修复 Provider Detail 在不同宿主模式下的返回链路和若干交互细节
- 修复附件在部分发送链路下重发不完整的问题，补齐请求附件准备流程
- 修复应用内提示反馈混用导致的返回体验不一致问题，普通提示回归 toast，操作型通知保留 snackbar
- 修复 MiniMax 兼容判断过宽的问题，避免用户修改网关后仍被误判为 MiniMax 专用链路

### 🔧 内部优化

- 收拢服务商兼容配置和错误提取逻辑，减少重复判断
- 清理一批未使用字符串资源，减轻资源噪音
- 继续收敛模型管理、聊天页和设置页的状态同步与显示细节

## v1.0.8 (2026-03-13)

这一版主要把模型管理和聊天列表这两条高频路径收稳了：服务商详情不再莫名收起，模型选择更清楚，导入导出也少了很多笨重的文件复制逻辑。

### ✨ 新增 / 改进

- 服务商模型选择器改成按品牌快速跳转，已选状态和分组展示更清晰
- 聊天里的模型选择器分组显示更稳定，不再偶发串到错误服务商名下
- 会话列表和消息列表补上更稳的主题刷新与缓存策略，长列表滚动更顺

### 🐛 修复

- 修复服务商管理里连续关闭再打开详情 sheet 时偶发自动收起的问题
- 修复聊天消息和会话卡片在主题切换、消息复用时状态串位的问题
- 修复导入导出附件时的大文件复制与会话摘要回填逻辑，降低失败概率
- 修复智谱模型分组命名和部分模型能力匹配覆盖自定义名称的问题

### 🔧 内部优化

- 导入导出统一改用 `fileIo.copyFile`，去掉多处手搓二进制拷贝
- 数据库消息导入支持延后刷新会话摘要，减少重复计算

## v1.0.5 (2026-03-06)

这一版主要解决数学公式显示和 AI 画图的问题，顺便修了个烦人的 bug。

### ✨ 新增 / 改进

- 聊天标题支持显示数学公式了（AI 起的标题带公式不再显示乱码）
- 会话列表卡片里的标题也能正确显示数学公式
- 消息气泡里的数学公式兼容更多模型格式（OpenAI 等用 `\[...\]` 写法的也能渲染了）
- 模型编辑页新增「图片生成」能力标签，可手动标记或自动识别
- 模型自动匹配更智能：中转站给模型加后缀也能正确识别能力了

### 🐛 修复

- 修复聊天卡片列表页长按功能失效的问题
- 修复部分 AI 画图模型返回图片链接（而非 base64）时图片无法显示的问题

### 🔧 内部优化

- AI 生成图片的保存逻辑统一抽取，消除了 4 处重复代码

## v1.0.4 (2026-03-04)

这一版主打「更顺手 + 更稳定」：模型学会分组列队、输出不抢你滚动、设置弹窗更可靠。

### ✨ 新增 / 改进

- 获取模型支持分组显示（找模型更快）
- 支持聊天字体大小调节（看得更舒服）
- 对话列表更紧凑：左右留白优化、左滑操作区更克制，FAB 更好点

### 🧠 体验优化

- 模型输出内容时不再影响用户上下滑动查看内容
- 服务商界面键盘避让，输入不再被挡住

### 🛠 稳定性与修复

- 设置页弹窗逻辑优化，更加稳定可靠
- 优化异常情况报错提示，更清晰可读
- 优化聊天界面大量消息时 jump 到底的逻辑，不再闪退
