# Changelog

## v2.0.10 (2026-09-18)

### ✨ 新增 / 改进

- 播报（Read Aloud）新增 MiMo TTS，支持 `mimo-v2.5-tts`、音色设计和声音克隆三个模型；提供九种预置音色、音色描述、MP3／WAV 参考音频导入及试听，长回复分段生成 MP3 并依次播放
- Python 工作区支持导入和处理 PDF、图片、Office、ZIP 等二进制文件，运行脚本时按原始字节同步文件；文件工具支持读取工作区路径，并在模型具备相应能力时提供原生多模态输入，PDF 可按页渲染后读取
- 工作区、聊天附件及文件工具统一采用 Material 文件图标，按文件名或扩展名区分类型，并适配深色模式
- 用量统计新增活动热力图与“全部／近 7 天／近 30 天”范围切换，支持左右滑动切换，概览、模型排行和费用统计使用一致的时间范围
- 思考与工具时间线包含三条及以上状态栏时支持折叠分组；优化合并栏的动态状态提示、长标题扫光和任务完成提示的沉浸光感外观
- 内置提示词与子智能体调度说明提供中英文版本，随应用语言切换；回复语言优先遵循用户要求，并在工具续轮和上下文压缩后保留语言偏好
- 调整工具执行与推理策略，减少重复检索和无效工具循环；开启知识库后由模型按问题需要决定是否检索，并完善推理档位控制

### 🐛 修复

- 修复主对话并发任务间上下文压缩状态相互覆盖、压缩结果丢失及重复压缩收益计算不准确的问题；压缩期间发送的消息进入队列，已开始的工具执行完成后再在请求边界压缩
- 子智能体新增独立上下文压缩，保留工具调用与结果的完整配对；避免子智能体预览内容重复进入主对话历史，并将自动压缩用量计入对应调用
- 修复已无对应会话的残留消息被计入用量和费用统计的问题，保留原始消息数据
- 修复知识库文件图标配色不一致、侧滑操作区域与卡片尺寸不匹配的问题
- 修复 API 播报快速停止或切换时，旧播放器的迟到错误可能影响新朗读的问题；播放器创建失败时清理音频文件及句柄，避免不同请求的临时音频文件相互覆盖

### 🔧 内部优化

- 减少长回复流式输出中的重复全文扫描、文本转换与时间线索引构建，复用消息渲染签名和工具索引，并合并上下文用量面板的重复刷新
- 新增 14 项 TTS 离线回归检查，覆盖 MiMo 请求格式、音频解码、参考音频导入、分段播放、异常与取消处理，以及本地和 ElevenLabs 引擎兼容性
- 应用版本升级为 2.0.10（versionCode：2000010），同步“关于”、设置页、MCP／邮件客户端、导出信息、许可证页及中英文 README 的项目版本徽章

## v2.0.1 (2026-09-11)

### ✨ 新增 / 改进

- 知识库与邮件分类改用完整文档卡片重叠的折叠态，展开／收起时卡片连续移动并恢复宽度；支持点击露出的卡边展开，保留文档预览、更新与删除侧滑操作
- 知识库文档卡片（含堆叠中的卡片）接入对话列表同款沉浸光感材质，沿用知识库材质开关；折叠与展开保持同一材质背板和背景颜色
- 精简知识库分类数量文案，移除“已展开全部”“已堆叠”；邮件统一显示“X 封邮件”，移除“尚未授权邮箱”整块提示区域
- lv-markdown-in 行内代码的英文、数字与代码符号采用等宽字体，中文继续使用当前正文或标题字体；保留嵌套强调、链接中的行内代码字形与背景样式
- 行内公式整体等比缩小约 10%，同步缩小排版占位；独立成行的公式保持原尺寸
- lv-markdown-in 的“复制／全选／取消”操作改由原生菜单承载，支持沉浸光感材质；低版本或主动降级时使用透明高斯模糊，保留原有选区与复制操作
- 聊天工具按钮行的左右渐隐调整至屏幕边缘，初始“工具”按钮与输入栏左边缘对齐；仅为模糊模式补充按钮阴影，沉浸光感模式保持不变

### 🐛 修复

- 修复 DeepSeek 同时开启思考和知识库时出现“Thinking mode does not support this tool_choice”的参数兼容问题：思考模式省略显式工具选择参数，保留工具定义与知识库调用校验；关闭思考及其他模型的行为保持不变
- 修复发送图片等附件后，短图文消息气泡因外层宽度约束而看起来居中的问题，使实际气泡与右侧对齐位置一致
- 修复模糊模式下底部导航选中滑块偶尔丢失过渡、直接跳到目标页签的问题；将动画直接绑定到滑块位移，不影响按钮点击区域

### 🔧 内部优化

- 完成 41 项 DeepSeek 请求序列化离线回归检查，覆盖思考开关、禁用工具、工具续轮推理内容回传、自定义协议覆盖，以及其他供应商与 Responses 协议不受影响的场景
- 补充 Markdown 字体回退、公式尺寸、原生菜单兼容分支及导航滑块连续切换的回归检查
- 应用版本升级为 2.0.1（versionCode：2000001），同步“关于”、设置页、MCP／邮件客户端、导出信息、许可证页及中英文 README 的版本标识

## v2.0.0 (2026-09-10)

### ✨ 新增 / 改进

#### 沉浸光感与界面

- 适配 HarmonyOS 7 对沉浸光感生效区域的限制，统一封装聊天输入框、输入／输出气泡、消息工具栏、提问弹窗、对话与知识库列表及多处按钮的材质效果
- “显示与外观”整理为“主题、字体、材质”三个页签；对话列表与知识库列表可分别切换沉浸材质，并新增“材质兼容性 → 关闭沉浸光感材质”选项，支持用户主动降级
- 材质新接口增加 `deviceInfo.apiAvailable('26.0.0')` 能力检测；接口不可用或用户主动关闭时改用透明高斯模糊，顶部导航与底部页签采用自绘降级外观，并关闭材质形变
- 为输入栏上方按钮、输入框内按钮、首页加号及消息工具栏补充光照与形变反馈；输入框本体仅保留光照、不参与形变，内部按钮独立响应
- 优化知识库与邮件分类的卡片堆叠、分类配色和错峰展开／收起动画；Markdown 代码块与行号同步跟随聊天字号、行高设置
- 输入栏下方新增带 AI 图标的“内容由 AI 生成；AI 也可能犯错”提示，可在固定位置长按隐藏或恢复，并带过渡动画
- 点击“关于 XCube”中的品牌图标可播放线条绘制动画，补齐实心 X 填色后渐隐恢复原始 Logo

#### 对话、搜索与工具

- 生成过程中可继续输入文字或添加附件，选择“引导当前回答”或“排队等待回答完成”；支持引导的调用链会在后续请求边界接收补充信息，排队消息在当前回答完成后发送
- 将连续的思考过程、工具调用、向用户提问与上下文压缩状态整合为统一结果块，保留展开详情；思考状态增加渐变扫光，Python 工具结果栏按具体文件或执行操作显示名称
- 新增 DeepSeek Web Search 搜索源，支持复用已配置的官方 DeepSeek 服务商凭据，或手动配置独立 API Key 与 Anthropic 兼容端点；搜索结果展示来源引用，并提示该能力会消耗模型 Token 额度
- 工具中心新增“每轮检索次数”设置，联网搜索与知识库分别支持不设限制或 1～10 次；主助手与每个子智能体独立计数，并保留结果去重与已有的预算确认机制
- 读取邮件时可同时返回受支持附件的本地文本提取或 OCR 结果，区分解析失败、不支持及内容截断；仍需用户逐次批准邮件读取
- 消息工具栏新增缓存命中率，按供应商实际用量区分普通输入、缓存读取与缓存写入，并保存到历史消息及导出数据
- 模型计费新增缓存命中价，可与原有峰谷时段价格共同配置；费用估算按独立缓存读取价计算命中部分，未命中、缓存写入及无法分类的估算输入仍按普通输入价计算
- 手动执行 `/compact` 时，在最近的助手气泡中显示“正在压缩上下文／已经压缩上下文”状态，并持久保存完成结果
- 主对话回复语言跟随当前用户提问，减少工具描述、网页、附件或引用内容中的语言对回复语言的干扰

#### Python 沙箱与协作工作区

- 新增按会话持久化的文本工作区，支持列出、分段读取、创建、覆盖、追加、精确修改和删除文件，以及依次运行多个 Python 入口脚本并读取同目录数据
- 工作区从聊天能力栏进入独立页面，提供目录树、文件筛选、带行号的代码预览与复制；长按可导入 UTF-8 文本文件、新建或删除文件／文件夹，入口随当前会话的工作区内容显示激活状态
- 主智能体与子智能体共享所属会话的 Python 工作区，同一工作区的操作串行执行；文件修改增加版本校验，避免协作时覆盖较新的内容
- Python 生成图可通过稳定附件引用直接嵌入回复正文，避免同时在附件区重复展示；生成的 PDF、CSV、JSON、Office、压缩包等文件可作为聊天内链接交付，并通过系统文件选择器另存
- 检测到 `import fpdf` 时自动安装 `fpdf2`；补齐 CMU Serif 与思源宋体的常规、粗体、斜体和粗斜体回退，改善 Matplotlib、Pillow 与 PDF 的中英混排
- Python 包下载与加载进度可实时显示；运行宿主跨页面导航保留，便于在对话与工作区之间切换时继续执行任务

### 🐛 修复

- 修复材质宿主的透明外扩区域和内建手势抢占触摸，导致工具按钮经常需要多次点击的问题；按钮内容、点击事件与材质反馈保持在同一交互主体上
- 修复多行输入时材质背板反过来锁定输入区高度的问题，恢复输入框按原生文本测量结果伸缩，并限制在设定的最小／最大高度内
- 修复导入／导出、WebDAV、字体档位、语音引擎与测试语音等按钮在沉浸模式下文字被底色遮挡、选中态难以辨认及白边异常的问题
- 修复进入聊天页时消息气泡、以及回到顶部／底部按钮出现时的首帧底色与阴影闪烁；调整材质留白、圆角和阴影边界，减少裁剪
- 修复顶部导航快速滚动时模糊不稳定，以及降级模式仍残留材质形变的问题；统一自绘底栏与加号的边框、阴影，移除首页四个选项的胶囊底色和底部多余明暗渐变，并校正图标垂直居中
- 修复 `ask_user` 参数到达后问题仍不显示或错误提示“参数不完整”的问题，兼容旧的单问题格式、二次 JSON 编码、缺失／重复问题 ID，并在同 ID 内容更新时刷新问题与选项
- 改善 OpenAI 兼容流式响应解析，兼容不同 SSE 换行、BOM、数组形式正文／推理及 Responses 事件；对未知网关避免发送其可能不支持的流式用量选项，并识别带路由前缀的推理模型名
- 修复工具参数增量与累计快照混用、重复片段及多轮调用状态混淆导致的 JSON 损坏；同时增强子智能体调度与地图参数的兼容解析，减少供应商格式差异造成的工具调用失败
- 修复知识库首次强制工具调用时对所有模型一律关闭推理的问题：仅对已知冲突协议临时关闭，其他模型保留用户推理设置；必要时进行一次受控降级重试
- 修复 DeepSeek 搜索依赖过期的当前服务商选择、误报未配置密钥的问题；自动模式只使用启用且指向官方端点的配置，避免将第三方代理密钥发送到官方服务
- 修复 Python 工作区 Worker 模板转义、重复建目录、输出目录变量不可见、修改模块后仍使用旧导入缓存，以及无实际变化的文本替换被误报成功等问题
- 修复 Python 宿主分离后请求继续等待、共享 Worker 执行相互干扰，以及工具初始化失败后状态未正确恢复的问题；补充执行队列、缓存刷新和失败清理
- 修复删除消息、消息分支或会话后普通文件与 Python 产物残留的问题；清理前复核引用并保护运行中任务及新文件，共享附件仍保留，已删除会话的迟到任务不能重新创建产物
- 修复知识库分类收起后层叠卡片互相覆盖、侵入上一张卡片区域的问题，改善展开／收起时的布局稳定性

### 🔧 内部优化

- 缓存每条消息的结果块布局，并复用时间线分组与过渡逻辑，减少流式输出中的重复布局计算
- 补充生成中引导、流式协议、工具参数、缓存计费、Python 工作区协作与清理等回归用例，以及自绘导航和沉浸按钮着色检查脚本
- 清理被跟踪的 Hvigor 构建产物，更新产品定位、中英文说明与界面截图，补充字体及参考实现的许可证署名；应用版本及相关客户端／导出版本标识同步为 2.0.0

## v1.3.6 (2026-08-26)

### ✨ 新增 / 改进

- 新增计划模式：模型可创建和更新多步骤任务计划，用户可通过 `/plan` 打开只读面板查看当前执行项与整体进度，会话重新打开后自动恢复
- 新增邮件能力：支持在工具中心配置 IMAP/SMTP 邮箱，通过逐次确认让模型读取或发送邮件，并可将邮件正文、附件和内嵌图片导入知识库；补充 Outlook OAuth2、网易邮箱客户端身份信息及系统安全资产凭据存储
- Python 沙箱升级为完整 Pyodide/CPython 运行时，自动加载 `pandas`、`numpy`、`scipy`、`scikit-learn`、`matplotlib`、Pillow 等官方包，也可安装纯 Python 或兼容 PyEmscripten wheel；模块 Worker 不兼容时自动回退经典 Worker 运行时
- Python 沙箱支持将 Matplotlib、Pillow 等生成的图片保存到当前会话附件沙箱；模型可通过附件 ID 继续读取，生成结果也会直接显示在聊天气泡中
- 上下文达到用户设定阈值时改由应用直接压缩；正文、思考或工具调用生成途中越过阈值都会立即中断，压缩后在同一个消息气泡中续答；压缩状态栏会跟随聊天的历史输出展示设置定位
- “关于 XCube”新增缓存清理入口，可扫描并删除无现有会话、消息或草稿引用的历史附件残留；执行前会明确提示永久删除与潜在数据丢失风险
- 服务商余额改为并行查询并逐项展示结果，单个服务商失败不会阻塞其他查询
- 图片预览页的关闭、页码和保存控件统一为模型能力激活态的主题色沉浸材质

### 🐛 修复

- 修复地图路线规划的瞬时失败、坐标解析与当前位置授权处理，提高驾车、步行和骑行路线的重试与错误提示稳定性
- 修复常用地点读取后的地图参数传递逻辑，并更正常用地点与地图工具的参数描述，避免把地点标签错误当作搜索词或路线坐标
- 修复带附件邮件在导入知识库时可能被重复导入的问题
- 修复删除单条会话后附件记录、工具事件、草稿附件、Python 生成图及沙箱文件没有一并清理的问题；共享文件仍有其他会话引用时会安全保留
- 修复 Canvas 与 Plan 入口布局：普通模式保持右侧竖列，手稿模式保持底部横排
- 修复 Python 运行时初始化、包加载或执行超时后无法可靠终止并重建的问题

### 🔧 内部优化

- 更新中英文 README、功能说明与项目截图，整理地图、知识库、子智能体和工具能力展示
- 补充 Python 运行时策略、地图路线、响应时间线及附件清理相关回归测试

## v1.3.5 (2026-08-24)

### 🐛 修复

- 模型上下文长度留空时统一按 256000 Token 计入上下文预算
- 自定义服务商名称输入文本后自动移除提示底色和边框

## v1.3.1 (2026-08-12)

### ✨ 新增 / 改进

- 新增 `/models`、`/reasoning`、`/canvas` 与 `/compact` 斜杠命令，可从输入框快速切换模型、调整推理强度、打开 Canvas 或压缩较早的对话历史
- 对话附件与知识库新增 DOCX、XLSX 支持：在本地解析 OOXML，DOCX 保留标题、段落、列表和表格，XLSX 保留工作表、单元格坐标、日期与公式缓存值；不支持原生文档阅读的模型可通过内置工具按需读取
- `web_search` 现在可以直接读取 JSON API URL，对返回数据自动格式化，并在内容过长时安全截断
- 重构模型与供应商图标解析，改用固定版本的 LobeHub Icons 静态 SVG，并扩展主流模型家族、聚合服务与自定义供应商的图标匹配及缓存回退
- 手稿模式新增生成中动态文字提示，补充思源宋体中文粗体与斜体字形，并优化助手署名、标题、文件附件及多处界面细节
- 模型编辑页新增上下文长度与最大输出 Token 配置；上下文面板会区分模型总窗口、有效输入预算及输出预留，并在发送前按实际输入自动收紧输出上限

### 🐛 修复

- 重构 Token 与上下文用量统计：改为基于最终请求中的系统提示、消息、附件文本、推理内容、工具 schema、工具调用和工具结果计算；多轮工具及子智能体请求优先采用 OpenAI、Anthropic、Google 返回的真实 `usage`，缺失时再使用本地估算
- 修复附件按文件字节数换算 Token 导致上下文虚高的问题；工具读取型附件现在只统计附加提示、工具定义和实际返回文本，原生附件由供应商处理并以其返回用量为准
- 修复附件对话执行 `/compact` 时误报 “No history yet” 或无法压缩的问题；摘要请求现在会保留附件记录、工具调用参数与实际工具结果，并等待最新上下文用量后再判断是否可压缩
- 修复未知上下文窗口被错误当作 4K、空白字符未计入估算、输出预算未从总窗口预留，以及工具历史协议还原不完整造成的统计偏差或请求失败
- 修复标题生成与重新生成偶发失败、状态卡住、错误被静默回退以及标题被强制截断的问题；标题请求改为隔离执行、限制超时并展示明确失败原因，长标题可滚动查看
- 修复重复导入知识库文件时仅检查关键词索引、遗漏失效或缺失向量的问题；现在会自动重建文档并补齐向量，同时在导入结果中显示真实索引状态
- 修复手稿模式中英文斜体缺失、英文底部标签及部分设置项在窄屏下溢出或截断的问题

## v1.3.0 (2026-08-10)

### ✨ 新增 / 改进

- 新增可在“设置 → 外观”中切换并持久保存的手稿模式，内置 Cormorant Garamond 与 Lora 字体，以书籍排版语言呈现界面
- 重构助手、对话、知识库和设置四个主标签页：采用扁平纸张背景、目录式列表、四栏页脚导航与独立的新建入口
- 重构手稿模式对话页：支持衬线标题与点击滚动、全宽消息排版、安全页边距、页码以及重新编排的顶部和侧边操作按钮
- 统一工具调用、工具结果、思考过程、向用户提问结果和消息操作栏的紧凑书页样式，并同步适配 Canvas、HTML、图表与搜索结果
- 重构手稿模式输入区和工具栏，统一工具、推理、知识库、附件与发送操作的布局和视觉语言
- 更新外观设置实时预览与中英文 README 截图，使普通模式和手稿模式均按当前界面呈现

### 🐛 修复

- 修复 `ask_user` 卡片背景异常透明，以及长工具栏、消息操作栏在窄屏设备上溢出或裁切的问题
- 修复英文环境下工具名称和结果缺失、`Reasoning` 被过早截断、字母间距过大及上下文压缩缺少英文名称的问题
- 修复普通模式工具、推理和知识库胶囊共用滚动容器导致阴影被裁切的问题，宽度不足时改为自然换行

## v1.2.2 (2026-08-04)

### ✨ 新增 / 改进

- 新增 lv-markdown-ui 渲染器
- 更新主题颜色，更具设计感
- “关于 XCube”页面新增检查更新入口，通过 GitHub Releases 获取最新正式版本、版本说明与更新状态
- 发现新版本时可直接前往 [XCube Releases](https://github.com/YANGZX22/XCube/releases) 查看并下载安装包
- “关于 XCube”页面新增开源许可证入口，以底部弹窗集中列出运行时依赖、内置模型、字体及开发工具的许可证与署名信息

## v1.2.1 (2026-08-03)

### ✨ 新增 / 改进

- 新增并行子智能体：主模型可一次派出最多 3 个隔离上下文的子智能体执行独立子任务，并通过实时预览面板查看输出、工具调用和运行状态
- 子智能体继承当前对话已开启的工具；主模型和每个子智能体各自拥有默认 5 次联网搜索额度，开启知识库时也各自拥最多 5 次 `knowledge_search` 查询额度
- 更新中英文 README 与默认 `app-help` Skill，补充子智能体的入口、适用场景和能力边界

### 🐛 修复

- 修复子智能体无法调用已开启知识库的问题，并为主模型与每个并行子智能体分别维护独立检索预算
- 加强知识库首轮必须调用的提示优先级，模型偶发未返回工具调用时自动重试一次
- `ask_user` 改为弹窗式逐个回答，多个待回答请求按后到先答处理；同一消息在主对话中合并为一个结果栏，展开后分段展示各次提问
- 聚合工具结果栏按整体结果显示状态：全部成功为绿色对号、成功失败并存为黄色叹号、全部失败为红色叉号

## v1.2.0 (2026-08-01)

本版重点重构知识库混合检索链路：向量近邻改为直接查询 ArkData COSINE 索引，两路召回在应用层融合，语义召回不再被中间层丢弃。

### ✨ 新增 / 改进

- 知识库混合检索重构：BM25 关键词与向量语义两路独立召回，在应用层做 union-RRF 融合排序，仅向量命中的结果不再被 Data Augmentation Kit 的最终相关性筛选淘汰
- 向量近邻查询直接走 ArkData GSDISKANN COSINE 索引（`<=>` + ORDER BY + LIMIT），并按 READY 文档范围过滤孤儿与旧 generation 数据
- 新增向量 sidecar 启动协调器与恢复策略，嵌入模型或维度变化时自动检测并重建向量索引
- 向量相似度阈值调整为 0.4，与 SDK 默认相关性分层的最低档对齐，中等相似度的语义命中可以参与融合

### 🐛 修复

- 修复真机 GaussPD 向量 ANN 查询使用绑定参数 LIMIT 时静默返回空集，导致向量召回始终为空的问题；并增加查询形状逐级降级兜底
- 修复含空格查询词（如 "ELEC 6099"）因分词不匹配 BM25 零命中、且向量命中被丢弃而检索不到结果的问题
- 修复向量维度不匹配、嵌入模型切换等场景下混合检索不能透明回退关键词检索的问题

### 🔧 内部优化

- 检索链路增加脱敏的计数级诊断日志（召回数、范围行数、最佳相似度），不输出查询词、正文与向量
- 新增检索计划、RRF 融合、恢复策略与启动协调器的守卫单测

## v1.1.18 (2026-07-31)

### ✨ 新增 / 改进

- Kimi 模型支持原生文档附件上传、内容提取与对话发送
- 进一步优化聊天输入、消息工具卡片、滚动按钮与主题界面体验

### 🐛 修复

- 修复扫描 PDF 的文本提取与 OCR 回退链路，提高无文本层文档的识别稳定性
- 修复知识库文档切块、向量写入和索引进度在大批量数据下的稳定性问题
- 修复模型支持文档但不支持图片时，附件能力被错误关闭或转入 PDF 工具的问题
- 修复部分界面状态冲突、附件发送失败和服务商余额展示异常

## v1.1.16 (2026-07-30)

- 应用品牌名由 ChatCube 更新为 XCube
- 通知、分享、日历、导出与 WebDAV 备份标识同步更新为 XCube
- 新增 XCube 浅紫主题并设为首次安装默认主题
- 移除应用内隐私政策入口与页面

## v1.1.0 (2026-04-30)

这一版从 `1.0.13` 继续往前收了一轮主体验：新增助手模块，把多助手管理、自定义助手头像、默认模型和系统提示词这些能力集中起来；聊天、设置与模型管理等高频页面统一换成更贴近 HarmonyOS 的 HDS 材质与渐变模糊；主题配色也重新整理，新增 Notion 主题。推理控制按不同服务商能力做了更细的适配，导入导出策略同步调整为恢复后可直接使用，为后续 WebDAV 全量同步打基础。

### ✨ 新增 / 改进

- 新增助手模块，支持在首页侧边入口管理多个助手，并按助手查看会话、快速创建专属对话
- 助手模块内支持自定义助手头像，可使用文字、图片链接、QQ 头像、本地图库图片和默认模型图标，列表页、详情页和编辑页的头像展示尺寸更统一
- 助手模块支持默认模型、系统提示词、温度覆盖等信息，默认助手和自定义助手的编辑、删除、排序与会话关联更清楚
- 聊天卡片页顶部接入 `HdsNavigation` 材质导航，搜索、收藏、标题栏与顶部渐变模糊范围重新整理，滚动时顶部质感更稳定
- 聊天 Tab 收敛为单 `HdsNavigation` 自适应分栏，大屏空态、返回路径和列表 / 详情切换更稳定
- 设置、模型管理、导入导出、关于、通知等二级页统一接入 HDS 顶部模糊效果，页面切换时材质表现更一致
- 主题配色重新整理，新增 Notion 极简主题，默认暖纸、Claude、ChatGPT、Violet、Mint 等主题的消息、卡片、Markdown 和桌面小组件配色更协调
- 首页筛选交互收敛，弱化「全部 / 收藏」Tab 的打扰，搜索展开与返回后的标题栏状态更稳
- 底部 / 侧边 Tab 补齐点击即时缩放反馈，助手 Tab 的选中态图标染色与对话页保持一致
- 推理控制按模型和服务商能力细分，补齐自动、低、中、高、最高等推理级别，并兼容更多 OpenAI 兼容服务商策略
- 新增小米 MiMo 预置服务商，并补齐 MiMo thinking / reasoning 兼容判断
- 导出数据默认包含 API Key、搜索服务和 MCP 连接配置，导入后可直接使用，同时在导出页明确提示备份文件需要保存到可信位置

### 🐛 修复

- 修复助手头像编辑后列表、设置页和对话入口不及时刷新的问题
- 修复聊天卡片页搜索展开后新建对话、返回列表时顶部搜索和收藏入口丢失的问题
- 修复搜索展开动画在状态恢复后缺失的问题
- 修复聊天卡片页左上角助手头像与助手设置页头像尺寸不一致的问题
- 修复会话列表多选、筛选和收藏状态切换时部分临时状态未正确回收的问题
- 修复推理级别在部分服务商和模型组合下可用性判断不准确的问题
- 修复部分组件仍读取旧主题色导致切换主题后颜色不一致的问题
- 修复后台任务完成后通知未及时清理的问题

### 🔧 内部优化

- 新增 `AssistantService`、助手头像缓存与助手管理页，减少首页对助手数据和编辑状态的直接耦合
- 补充助手、头像、服务商、偏好设置在导入导出中的结构，为 WebDAV 全量快照同步预留统一备份基础
- 抽出 HDS 顶部模糊、点光效果和聊天顶部导航相关状态，减少导航栏、搜索栏、筛选栏之间的布局联动成本
- 重整主题色服务与全局 UI 状态，减少主题资源、页面硬编码颜色和桌面小组件之间的同步成本
- 扩展 `ProviderCompatibilityRegistry`，把不同服务商的推理请求策略集中维护，降低发送链路里的特殊判断

## v1.0.13 (2026-04-24)

这一版聚焦聊天输入和消息复制两条高频体验，同时把 MCP 工具发送流程、首页悬浮按钮和主题色透明度处理继续收紧。整体变化不大，但都落在每天会碰到的细节上。

### ✨ 新增 / 改进

- 新增「聊天输入」设置页，可选择键盘回车键行为：默认换行，或开启后直接作为发送按钮
- 助手消息新增原地文本选择模式，可通过长按消息内容或更多菜单里的「选择文本」进入
- 文本选择模式下会保留 Markdown 的基础阅读样式，并在复制时整理为更干净的纯文本
- 首页「新对话」悬浮按钮调整为更大的圆形按钮，补齐按压弹簧动画、阴影和底部占位
- 主题色透明度拼接统一改为 `withColorAlpha` 工具，输入框、消息气泡、工具中心和滚动按钮等状态色更稳定

### 🐛 修复

- 修复助手消息分段复制不完整、富文本选择状态样式不统一的问题
- 修复选择文本弹窗颜色资源和按钮布局细节，选择态气泡更清楚
- 修复 MCP 工具未完全就绪时发送被直接拦截的问题，改为后台刷新可用状态
- 修复部分主题色带透明度时在不同组件中显示不一致的问题

### 🔧 内部优化

- 新增 `SelectableMarkdownContent`，把选择态 Markdown 渲染和纯文本提取逻辑独立出来
- 聊天输入设置接入 `PreferencesService`、`SettingsManager` 和全局 UI 状态，保证设置即时生效
- `docs/appgallery` 加入忽略，避免应用市场截图产物误提交

## v1.0.12 (2026-04-13)

这一版继续围绕高频路径做收口，不是单点功能堆砌，而是把聊天详情页标题栏、多选链路、设置下钻页、图片预览、附件面板和视觉材质这些每天都会碰到的地方再打磨一轮。整体方向很明确：继续基于鸿蒙 6（API 23）把常用路径做顺、做稳、做干净。

### ✨ 新增 / 改进

- 聊天详情页标题栏完成一轮重构，HDS 风格布局、模糊范围、操作菜单、图标尺寸与滚动联动更统一
- 设置界面继续减负，下钻页隐藏不必要的 Tabs，底部占位与回退路径更清楚
- 聊天界面与消息/会话列表的多选能力进一步补齐，悬浮批量工具栏和相关交互更顺手
- 聊天页附件面板与加号切换逻辑优化，输入框回车发送能力补上
- 联网搜索能力补齐 Bing(local)、Tavily、Exa 三种执行引擎，聊天内搜索卡片会展示实际使用的搜索源
- 工具中心继续扩展，支持远端 streamable MCP Server 接入，统一承接内置工具与远端工具能力
- ChatGPT 主题与整体配色继续微调，会话列表层级和正文可读性进一步优化
- 聊天流式请求读取超时时间提升到 300 秒，长回复和慢流式场景更稳
- 增加沉浸光感材质设置项，8 种配色主题、悬浮按钮、滚动按钮、桌面卡片和整体主题视觉继续统一
- 服务商兼容层补充 OpenAI Responses transport、导入保留 transport 与排序信息，模型与服务商管理体验更完整

### 🐛 修复

- 修复聊天图片区域滑动手势被吞、图片预览状态栏切换不稳的问题
- 修复聊天界面、消息卡片和会话列表在多选模式下的一批状态错乱、布局不稳和交互别扭问题
- 修复等待 AI 回复后的 typing indicator 恢复、流式输出中消息工具栏过早出现等聊天细节问题
- 修复设置下钻页 HdsTabs 底部显示与占位异常，以及部分 overlay / blur 叠层关系不稳定的问题
- 修复冷启动时状态栏颜色与沉浸式显示偶发不一致的问题
- 修复 FAB 阴影、按钮边框激活态位移、桌面卡片深浅色跟随系统等一批视觉细节问题

### 🔧 内部优化

- 继续收缩 `ChatPage`、`Index` 和标题栏相关状态分支，降低复杂 UI 的维护成本
- Provider 导入排序、列表渲染、标题栏交互和多选逻辑补上更多回归边界
- 补充 `.gitattributes` 统一 LF 行尾，顺手清理一批没用的配置与入口

## v1.0.11 (2026-03-31)

这一版最核心的变化不是某个按钮或某个页面，而是运行门槛正式提升到了鸿蒙 6（API 23）。与此同时，首页、聊天页和服务商配置这几条高频路径也继续收紧：主界面的两栏导航更干净，遮罩和浮层不再抢戏，服务商和 MCP 都补上了更克制的自定义请求头能力。

### ✨ 新增 / 改进

- 应用运行前提调整为鸿蒙 6（API 23），`targetSdkVersion` 与 `compatibleSdkVersion` 统一升级到 `6.1.0(23)`
- 服务商级别新增自定义请求头能力，聊天发送、测试连接、获取模型、标题生成、翻译和搜索优化等链路统一生效
- MCP 工具服务的自定义请求头编辑体验同步收紧，和服务商配置保持一致
- 首页底部导航收敛为双标签布局，设置入口更集中，整体层级更清楚
- 聊天页和首页的一批遮罩、悬浮元素与覆盖层进一步压平，视觉更干净，交互也更稳
- 设置与隐私相关导航继续收口，二级页跳转更顺，回退路径更统一

### 🐛 修复

- 修复首页和聊天页多处 overlay/blur 叠层关系不稳定、偶发抢焦点和遮挡内容的问题
- 修复服务商自定义请求头开启后自动插入空白项，关闭后仍被错误计数的问题
- 修复请求头编辑区在展开后把开关位置挤偏的问题，开关位置现已固定
- 修复聊天与会话列表在高频更新场景下的一批渲染抖动、复用不稳和局部刷新噪音

### 🔧 内部优化

- 继续收缩首页、聊天页和列表项的渲染负担，减少不必要的重建与状态扩散
- 清理一批已废弃字符串资源和未使用入口，减轻资源与界面噪音
- 自定义请求头的解析、校验和敏感字段判断抽成共享工具，避免服务商与 MCP 两套逻辑继续分叉

## v1.0.10 (2026-03-20)

这一版不是修修补补，而是把工具链、列表渲染和服务商兼容层一起收了一轮：MCP 工具中心正式落地，聊天内工具调用状态更稳定，会话/消息列表的数据源与复用链路也更清晰，整体可维护性比之前高一个量级。

### ✨ 新增 / 改进

- 新增 MCP 服务接入和工具中心主流程，统一承接内置工具与远端 MCP 工具
- 聊天页补齐工具选择、审批和执行相关 UI，工具栏 pills 与工具调用卡片展示更稳定
- 推理能力配置从服务商策略中解耦，模型编辑页和聊天页按模型真实能力展示 reasoning 相关入口
- 会话列表和消息列表引入更清晰的数据源与列表项结构，长列表滚动和复用表现更稳
- 隐私政策改为应用内二级页面承载，避免跳转外部浏览器打断当前设置流
- 聊天界面补充进度圈元素，方便更直观地感知当前浏览位置
- 分享图片现在支持更长的对话内容，长对话导出不再容易被截断

### 🐛 修复

- 修复工具事件、工具状态在发送流程、落库恢复和消息 UI 之间不同步的问题
- 修复服务商兼容判断、模型获取和分享预览链路中的一批分叉问题，provider 图标和分享内容更稳定
- 修复首页 session list 更新链路里未读角标和多处冗余状态带来的刷新噪音
- 修复消息列表复用、滚动联动和部分工具消息卡片在复杂场景下的渲染异常
- 修复等待 AI 回复时 typing indicator 三点动画丢失的问题
- 修复首页搜索展开后输入框不自动聚焦、软键盘不弹出的交互回归
- 修复系统提示词、显示与外观、工具服务等设置页底部内容与 compact tabs 贴边甚至被遮挡的问题
- 修复聊天界面进度圈依赖绝对滚动偏移时在复杂列表下不够稳定的问题，改为基于消息可见位置计算

### 🔧 内部优化

- 新增 `ProviderCompatibilityRegistry`、`McpService`、列表 DataSource 等基础设施，明显减轻 `ChatPage` 和 `Index` 的状态负担
- 重构聊天工具相关 sheet 组件，减少 `ChatPage` 膨胀，降低后续继续堆特殊分支的风险
- 补充 `RefactorGuard.test.ets` 和 `ohosTest` 入口，为本轮结构调整加上基础回归边界

## v1.0.9 (2026-03-16)

这一版主要把服务商配置、聊天发送稳定性和提示反馈再收紧一轮：服务商详情页更顺手，附件重发和返回提示更可靠，MiniMax 兼容链路也补齐了。

### ✨ 新增 / 改进

- 服务商详情页的标题与地址信息对齐优化，编辑和查看状态更统一
- MiniMax 预置服务商升级到最新兼容地址，连接测试与模型获取流程更顺
- 服务商测试连接、获取模型失败时优先展示真实错误信息，排查更直接

### 🐛 修复

- 修复会话重新打开时总是回到全局当前模型的问题，改为优先恢复该会话上次使用的模型
- 修复服务商详情页标题首字显示不完整、URL 摘要未对齐的问题
- 修复 Provider Detail 在不同宿主模式下的返回链路和若干交互细节
- 修复附件在部分发送链路下重发不完整的问题，补齐请求附件准备流程
- 修复应用内提示反馈混用导致的返回体验不一致问题，普通提示回归 toast，操作型通知保留 snackbar
- 修复 MiniMax 兼容判断过宽的问题，避免用户修改网关后仍被误判为 MiniMax 专用链路

### 🔧 内部优化

- 收拢服务商兼容配置和错误提取逻辑，减少重复判断
- 清理一批未使用字符串资源，减轻资源噪音
- 继续收敛模型管理、聊天页和设置页的状态同步与显示细节

## v1.0.8 (2026-03-13)

这一版主要把模型管理和聊天列表这两条高频路径收稳了：服务商详情不再莫名收起，模型选择更清楚，导入导出也少了很多笨重的文件复制逻辑。

### ✨ 新增 / 改进

- 服务商模型选择器改成按品牌快速跳转，已选状态和分组展示更清晰
- 聊天里的模型选择器分组显示更稳定，不再偶发串到错误服务商名下
- 会话列表和消息列表补上更稳的主题刷新与缓存策略，长列表滚动更顺

### 🐛 修复

- 修复服务商管理里连续关闭再打开详情 sheet 时偶发自动收起的问题
- 修复聊天消息和会话卡片在主题切换、消息复用时状态串位的问题
- 修复导入导出附件时的大文件复制与会话摘要回填逻辑，降低失败概率
- 修复智谱模型分组命名和部分模型能力匹配覆盖自定义名称的问题

### 🔧 内部优化

- 导入导出统一改用 `fileIo.copyFile`，去掉多处手搓二进制拷贝
- 数据库消息导入支持延后刷新会话摘要，减少重复计算

## v1.0.5 (2026-03-06)

这一版主要解决数学公式显示和 AI 画图的问题，顺便修了个烦人的 bug。

### ✨ 新增 / 改进

- 聊天标题支持显示数学公式了（AI 起的标题带公式不再显示乱码）
- 会话列表卡片里的标题也能正确显示数学公式
- 消息气泡里的数学公式兼容更多模型格式（OpenAI 等用 `\[...\]` 写法的也能渲染了）
- 模型编辑页新增「图片生成」能力标签，可手动标记或自动识别
- 模型自动匹配更智能：中转站给模型加后缀也能正确识别能力了

### 🐛 修复

- 修复聊天卡片列表页长按功能失效的问题
- 修复部分 AI 画图模型返回图片链接（而非 base64）时图片无法显示的问题

### 🔧 内部优化

- AI 生成图片的保存逻辑统一抽取，消除了 4 处重复代码

## v1.0.4 (2026-03-04)

这一版主打「更顺手 + 更稳定」：模型学会分组列队、输出不抢你滚动、设置弹窗更可靠。

### ✨ 新增 / 改进

- 获取模型支持分组显示（找模型更快）
- 支持聊天字体大小调节（看得更舒服）
- 对话列表更紧凑：左右留白优化、左滑操作区更克制，FAB 更好点

### 🧠 体验优化

- 模型输出内容时不再影响用户上下滑动查看内容
- 服务商界面键盘避让，输入不再被挡住

### 🛠 稳定性与修复

- 设置页弹窗逻辑优化，更加稳定可靠
- 优化异常情况报错提示，更清晰可读
- 优化聊天界面大量消息时 jump 到底的逻辑，不再闪退
